<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Editar Tarefa</title>
</head>
<body>
  <h2>Editar Tarefa</h2>
  <form action="/tarefas/<%= tarefa.id %>?_method=PUT" method="POST">
    <label>Título:</label>
    <input type="text" name="titulo" value="<%= tarefa.titulo %>" required><br>
    <label>Descrição:</label>
    <input type="text" name="descricao" value="<%= tarefa.descricao %>" required><br>
    <label>Status:</label>
    <select name="status">
      <option value="pendente" <%= tarefa.status === 'pendente' ? 'selected' : '' %>>Pendente</option>
      <option value="concluida" <%= tarefa.status === 'concluida' ? 'selected' : '' %>>Concluída</option>
    </select><br>
    <label>Data Limite:</label>
    <input type="date" name="data_limite" value="<%= tarefa.data_limite.toISOString().slice(0,10) %>" required><br>
    <label>Matéria ID:</label>
    <input type="number" name="materia_id" value="<%= tarefa.materia_id %>" required><br>
    <button type="submit">Salvar</button>
  </form>
  <a href="/Gerenciamento">Voltar</a>
</body>
</html>